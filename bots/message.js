export default class Message 
{
    constructor(senderName, senderAvatar, content, dateTime)
    {
        this.senderName = senderName;
        this.senderAvatar = senderAvatar;
        this.content = content;
        this.dateTime = dateTime;
    }
}